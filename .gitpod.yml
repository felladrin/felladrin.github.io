image:
  file: .gitpod.Dockerfile
tasks:
  - name: Dev Server
    init: npm ci
    command: |
      npx cypress install
      npm run dev
ports:
  - name: Dev Server
    port: 1234
    onOpen: open-preview
